{
    "name": "matheus85/duat",
    "description": "Unified resilience patterns for modern PHP: retry, circuit breaker, timeout, bulkhead, rate limiter and fallback. Zero required dependencies.",
    "type": "library",
    "license": "MIT",
    "keywords": [
        "resilience",
        "retry",
        "circuit-breaker",
        "rate-limiter",
        "bulkhead",
        "timeout",
        "fallback",
        "backoff"
    ],
    "authors": [
        {
            "name": "Matheus Marques"
        }
    ],
    "require": {
        "php": ">=8.3"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.75",
        "phpstan/phpstan": "^2.1",
        "phpunit/phpunit": "^11.5 || ^12.0",
        "psr/event-dispatcher": "^1.0",
        "psr/simple-cache": "^3.0"
    },
    "suggest": {
        "psr/simple-cache": "To use any PSR-16 cache as shared state store",
        "psr/event-dispatcher": "To receive resilience events (circuit opened, retry attempted...)",
        "ext-redis": "Recommended production state store with atomic operations"
    },
    "autoload": {
        "psr-4": {
            "Duat\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Duat\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "test": "phpunit",
        "stan": "phpstan analyse",
        "cs": "php-cs-fixer fix --dry-run --diff",
        "cs:fix": "php-cs-fixer fix"
    }
}
