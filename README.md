# Duat

Unified resilience patterns for modern PHP: retry, circuit breaker, timeout,
bulkhead, rate limiter and fallback. Zero required dependencies.

Work in progress. The first release (0.1.0) will ship the core policies and
the fluent API.
